services:
  - type: web
    name: pons-api
    runtime: node
    plan: starter
    buildCommand: cd apps/api && pnpm install && pnpm run build
    startCommand: cd apps/api && pnpm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001
