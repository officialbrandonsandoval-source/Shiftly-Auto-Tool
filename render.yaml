services:
  - type: web
    name: pons-api
    runtime: node
    plan: starter
    region: oregon
    buildCommand: |
      corepack enable
      pnpm install --frozen-lockfile
      pnpm --filter pons-api run build
    startCommand: |
      pnpm --filter pons-api start
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production